# 🏦 Проверка ковенантов банка с LangChain

**Современная система автоматической проверки соответствия заявок минимальным ковенантам банка с использованием трех техник промптинга**

## Минимальный каркас для автоматизации процесса проверки ковенантов в проектном финансировании.

## 🚀 Возможности

* **Три техники промптинга** — Zero-Shot, Few-Shot, Chain-of-Verification
* **Автоматическая валидация** — через Pydantic модели
* **JSON-выход** — для интеграции с CRM и дашбордами
* **Объяснимость** — детальные объяснения по каждой метрике
* **Обработка ошибок** — с автоматическими повторными попытками
* **Масштабируемость** — обработка множества заявок 24/7

---

## 📁 Структура проекта

```
pf_covenants_checker/
├─ .env                    # Переменные окружения
├─ .gitignore             # Игнорируемые файлы
├─ requirements.txt       # Зависимости Python
├─ run.py                 # Простой скрипт для запуска
├─ main.py                # CLI интерфейс (сложный)
├─ prompts.py             # Промпты для трех техник
├─ chain_factory.py       # Фабрика LangChain цепочек
├─ validators.py          # Pydantic модели
├─ examples/
│  ├─ ok.json             # Пример проходящей заявки
│  └─ fail.json           # Пример отклоненной заявки
└─ README.md              # Документация
```

---

## ⚙️ Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/pf_covenants_checker.git
cd pf_covenants_checker
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` и добавьте ваши ключи:

```env
OPENAI_API_KEY=sk-ваш-ключ-здесь
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL_NAME=gpt-4o-mini
TEMPERATURE=0.2
```

---

## 🎯 Использование

### Простые команды

#### Zero-Shot проверка
```bash
# С файлом
python run.py zero --input examples/ok.json

# Интерактивный ввод
python run.py zero
```

#### Few-Shot проверка
```bash
# С файлом
python run.py few --input examples/fail.json

# Интерактивный ввод
python run.py few
```

#### Chain-of-Verification проверка
```bash
# С файлом
python run.py cov --input examples/ok.json

# Интерактивный ввод
python run.py cov
```

### Дополнительные опции

- `--explain` - показать человеко-читаемые объяснения
- `--temperature` - изменить температуру модели
- `--input` - загрузить данные из JSON файла

### Примеры использования

```bash
# Проверка с объяснениями
python run.py zero --input examples/ok.json --explain

# Изменение температуры
python run.py few --input examples/fail.json --temperature 0.5

# Chain-of-Verification с файлом
python run.py cov --input examples/ok.json --explain
```

---

## 📊 Правила проверки ковенантов

Система проверяет четыре метрики:

- **LTV (Loan-to-Value)**: должно быть < 0.7
- **LLCR (Loan Life Coverage Ratio)**: должно быть ≥ 1.0  
- **DSCR (Debt Service Coverage Ratio)**: должно быть > 1.1
- **Balloon (Balloon payment %)**: должно быть ≤ 50

---

## 🔧 Техники промптинга

### 1. Zero-Shot
Строгие инструкции с четкими правилами сравнения. Модель получает только правила и входные данные.

### 2. Few-Shot  
Добавлены 2-3 эталонных примера с граничными случаями для лучшего понимания задачи.

### 3. Chain-of-Verification
Модель выполняет самопроверку перед финальным ответом, проверяя корректность всех сравнений и граничных условий.

---

## 📋 Формат результата

```json
{
  "per_metric": {
    "ltv": "OK/FAIL",
    "llcr": "OK/FAIL", 
    "dscr": "OK/FAIL",
    "balloon": "OK/FAIL"
  },
  "explanations": {
    "ltv": "краткое объяснение",
    "llcr": "краткое объяснение",
    "dscr": "краткое объяснение", 
    "balloon": "краткое объяснение"
  },
  "verdict": "APPROVE/REJECT"
}
```

---

## 🛠 Технологии

* **Python 3.11+** — основной язык
* **LangChain** — фреймворк для LLM приложений
* **OpenAI API** — работа с GPT моделями
* **Pydantic** — валидация данных
* **Typer** — CLI интерфейс
* **python-dotenv** — управление переменными окружения

---

## 🚨 Устранение неполадок

**Ошибка "OPENAI_API_KEY не установлен":**

* Убедитесь, что файл `.env` создан
* Проверьте правильность API ключей
* Убедитесь, что `OPENAI_BASE_URL` указан явно

**Ошибка "Connection error":**

* Проверьте интернет-соединение
* Убедитесь, что API ключ действителен
* Проверьте лимиты использования API

**Ошибка валидации JSON:**

* Система автоматически повторяет попытку
* Проверьте корректность входных данных

---

## 📝 Лицензия

Этот проект создан в образовательных целях. Используйте ответственно и в соответствии с условиями использования OpenAI API.

---

## 🤝 Вклад в проект

Приветствуются предложения по улучшению:

* Добавление новых техник промптинга
* Улучшение обработки ошибок
* Добавление логирования
* Оптимизация производительности

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте раздел "Устранение неполадок"
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность API ключей
4. Создайте issue в репозитории

---

## 📬 Контакты

* **GitHub:** your-username
* **Email:** your-email@example.com

---

_Создано с ❤️ для изучения техник промптинга в LangChain_

---

## 🎯 Цель проекта

Создать **простой и современный инструмент** для автоматизации проверки ковенантов банка.  
Каждая заявка — новая возможность применить AI для решения реальных бизнес-задач.

---

🏦 **Главное — автоматизировать рутинные решения. Остальное можно оптимизировать.**

---

## 💼 Как цепочка помогает решать профессиональную задачу

### 🏦 **Профессиональная задача:**
Автоматическая проверка соответствия заявки минимальным ковенантам банка (LTV < 0.7, LLCR ≥ 1.0, DSCR > 1.1, Balloon ≤ 50)

### ⚡ **Как цепочка решает задачу:**

1. **Стандартизация** - единые правила без человеческого фактора
2. **Масштабируемость** - обработка множества заявок 24/7  
3. **Объяснимость** - детальные объяснения по каждой метрике
4. **Интеграция** - JSON-выход для систем банка

### 🔗 **Преимущества LangChain:**
- Автоматическая валидация через Pydantic
- Обработка ошибок с retry
- Структурированный JSON вместо текста
- Переиспользование для разных заявок

**Цепочка автоматизирует проверку ковенантов, обеспечивая стандартизацию, масштабируемость и надежность процесса принятия решений.** 🏆